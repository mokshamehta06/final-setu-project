<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | SETU</title>
  <!-- Google Fonts & Font Awesome -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Custom Design Tokens & UI -->
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/animations.css">
  <link rel="stylesheet" href="/css/buttons.css">
  <link rel="stylesheet" href="/css/ui-enhancements.css">
  <link rel="stylesheet" href="/css/chatbot.css">
  <style>
    .form-group {
  margin-bottom: 1.5rem;
  position: relative;
}

.form-control {
  width: 100%;
  padding: 0.75rem 1rem;
  font-size: 1rem;
  border: 1px solid #ddd;
  border-radius: 4px;
  transition: border-color 0.3s ease;
}

.form-control.error {
  border-color: #dc3545;
}

.error-message {
  color: #dc3545;
  font-size: 0.875rem;
  margin-top: 0.25rem;
  min-height: 1.25rem;
}

.hidden {
  display: none;
}

.alert {
  padding: 1rem;
  margin-bottom: 1rem;
  border-radius: 4px;
}

.alert-danger {
  background-color: #f8d7da;
  border: 1px solid #f5c6cb;
  color: #721c24;
}

.alert-success {
  background-color: #d4edda;
  border: 1px solid #c3e6cb;
  color: #155724;
}

/* Password strength indicator */
.password-strength {
  height: 4px;
  margin-top: 0.5rem;
  border-radius: 2px;
  transition: all 0.3s ease;
}

.password-strength.weak {
  background-color: #dc3545;
  width: 33%;
}

.password-strength.medium {
  background-color: #ffc107;
  width: 66%;
}

.password-strength.strong {
  background-color: #28a745;
  width: 100%;
}

  </style>
</head>
<body>
  <% if(page !== 'customer-login' && page !== 'customer-register' && page !== 'agency-login' && page !== 'agency-register') { %>
    <%- include('partials/header') %>
  <% } %>

  <!-- Flash Messages -->
  <% if(success_msg && success_msg.length > 0) { %>
    <div class="notification success visible">
      <%= success_msg %>
    </div>
  <% } %>

  <% if(error_msg && error_msg.length > 0) { %>
    <div class="notification error visible">
      <%= error_msg %>
    </div>
  <% } %>

  <!-- Home Page -->
  <% if(page === 'home') { %>

    <!-- Announcement Bar -->
    <div class="announcement-bar">
      <div class="container-fluid d-flex align-items-center justify-content-center gap-2 py-2 px-3 text-center text-white small fw-bold">
        <span class="announcement-badge"><i class="fas fa-shield-halved text-warning me-1"></i> OFFICIAL DISPOSAL</span>
        <span class="announcement-text">Direct Public Clearance Lot &bull; All Confiscated Assets 100% Inspected &bull; Free Express Delivery</span>
      </div>
    </div>

    <!-- E-Commerce Hero Section -->
    <section class="ecom-hero-section position-relative overflow-hidden">
      <div class="hero-bg-glow"></div>
      <div class="container py-5 position-relative" style="z-index: 2;">
        <div class="row align-items-center g-5">
          <div class="col-lg-7">
            <div class="hero-verified-badge mb-3">
              <span class="pulse-dot"></span>
              <i class="fas fa-certificate text-warning"></i>
              <span class="text-uppercase">Direct Citizen Access &bull; Fixed Clearance Rates</span>
            </div>

            <h1 class="hero-title">
              Legally Seized Goods.<br>
              <span class="hero-title-gradient">Direct Clearance Prices.</span>
            </h1>

            <p class="hero-subtitle mb-4">
              Direct public resale of authentic goods confiscated during customs checks, tax enforcement, and court asset liquidations. Certified authentic, tested, and backed by escrow protection.
            </p>

            <!-- Search Bar in Hero -->
            <form action="/customer/search" method="POST" class="hero-search-form mb-4">
              <div class="hero-search-box">
                <i class="fas fa-search hero-search-icon"></i>
                <input type="text" name="searchTerm" class="hero-search-input" 
                       placeholder="Search seized MacBooks, iPhones, cameras, consoles..." 
                       aria-label="Search seized inventory">
                <button type="submit" class="btn btn-hero-search">
                  <span>Find Deals</span>
                  <i class="fas fa-arrow-right ms-1"></i>
                </button>
              </div>
            </form>

            <!-- Popular Quick Filter Tags -->
            <div class="hero-quick-tags d-flex align-items-center gap-2 flex-wrap">
              <span class="tags-label"><i class="fas fa-fire text-warning me-1"></i> Popular:</span>
              <a href="/customer/browsing/laptops" class="hero-chip">💻 MacBooks</a>
              <a href="/customer/browsing/mobile" class="hero-chip">📱 iPhones</a>
              <a href="/customer/browsing/electronics" class="hero-chip">🎧 Sony XM5</a>
              <a href="/customer/browsing/cameras" class="hero-chip">📷 Canon R5</a>
              <a href="/customer/browsing" class="hero-chip hero-chip-all">Explore All &rarr;</a>
            </div>
          </div>

          <!-- Hero Spotlight Deal Card -->
          <div class="col-lg-5">
            <div class="spotlight-deal-card">
              <div class="spotlight-image-wrapper">
                <span class="spotlight-badge">
                  <i class="fas fa-fire me-1"></i> TOP SEIZED DEAL
                </span>
                <img src="https://images.unsplash.com/photo-1517336714731-489689fd1ca8?w=800&auto=format&fit=crop&q=80" 
                     alt="Apple MacBook Pro 16 M2 Pro" 
                     class="spotlight-img">
              </div>
              <div class="spotlight-content">
                <div class="d-flex justify-content-between align-items-center mb-2">
                  <span class="spotlight-tag">Customs Seizure Lot</span>
                  <div class="spotlight-rating">
                    <i class="fas fa-star text-warning"></i>
                    <i class="fas fa-star text-warning"></i>
                    <i class="fas fa-star text-warning"></i>
                    <i class="fas fa-star text-warning"></i>
                    <i class="fas fa-star text-warning"></i>
                    <span class="rating-val ms-1">4.9 (128 reviews)</span>
                  </div>
                </div>
                <h4 class="spotlight-title">Apple MacBook Pro 16" M2 Pro (16GB / 512GB)</h4>
                <div class="spotlight-pricing d-flex align-items-baseline gap-3 mb-3">
                  <span class="current-price">₹84,999</span>
                  <span class="original-price">₹1,69,900</span>
                  <span class="discount-badge">Save 50%</span>
                </div>
                <div class="d-grid">
                  <a href="/customer/browsing" class="btn btn-spotlight-cta">
                    <i class="fas fa-shopping-cart me-2"></i> Browse All Seized Stock
                  </a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Category Quick Navigation Bar -->
    <section class="category-nav-section py-4 bg-white border-bottom">
      <div class="container">
        <div class="row g-3 text-center justify-content-center">
          <div class="col-6 col-md-4 col-lg-2">
            <a href="/customer/browsing/laptops" class="category-card">
              <div class="cat-icon-circle"><i class="fas fa-laptop"></i></div>
              <div class="cat-name">Laptops & PCs</div>
              <span class="cat-price-hint">From ₹64,999</span>
            </a>
          </div>
          <div class="col-6 col-md-4 col-lg-2">
            <a href="/customer/browsing/mobile" class="category-card">
              <div class="cat-icon-circle"><i class="fas fa-mobile-alt"></i></div>
              <div class="cat-name">Smartphones</div>
              <span class="cat-price-hint">From ₹59,999</span>
            </a>
          </div>
          <div class="col-6 col-md-4 col-lg-2">
            <a href="/customer/browsing/electronics" class="category-card">
              <div class="cat-icon-circle"><i class="fas fa-headphones"></i></div>
              <div class="cat-name">Audio & Tech</div>
              <span class="cat-price-hint">From ₹11,499</span>
            </a>
          </div>
          <div class="col-6 col-md-4 col-lg-2">
            <a href="/customer/browsing/cameras" class="category-card">
              <div class="cat-icon-circle"><i class="fas fa-camera"></i></div>
              <div class="cat-name">Cameras & Gear</div>
              <span class="cat-price-hint">From ₹54,999</span>
            </a>
          </div>
          <div class="col-6 col-md-4 col-lg-2">
            <a href="/customer/browsing" class="category-card">
              <div class="cat-icon-circle"><i class="fas fa-gamepad"></i></div>
              <div class="cat-name">Gaming Gear</div>
              <span class="cat-price-hint">From ₹29,999</span>
            </a>
          </div>
          <div class="col-6 col-md-4 col-lg-2">
            <a href="/customer/browsing" class="category-card category-card-highlight">
              <div class="cat-icon-circle highlight"><i class="fas fa-store"></i></div>
              <div class="cat-name">All Categories</div>
              <span class="cat-price-hint highlight">Explore 200+ &rarr;</span>
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- Live Seized Products Showcase -->
    <section class="products-showcase-section py-5">
      <div class="container">
        <div class="showcase-header d-flex justify-content-between align-items-end mb-4 flex-wrap gap-3">
          <div>
            <div class="showcase-badge mb-2">
              <i class="fas fa-bolt text-warning me-1"></i> Authentic Confiscations
            </div>
            <h2 class="showcase-title mb-1">Today's Verified Seized Deals</h2>
            <p class="showcase-subtitle mb-0">Inspected by accredited disposal agencies. Verified chain of custody.</p>
          </div>
          <a href="/customer/browsing" class="btn btn-view-all">
            <span>View All <%= (typeof products !== 'undefined' && products) ? products.length : 12 %> Products</span>
            <i class="fas fa-arrow-right ms-2"></i>
          </a>
        </div>

        <% if (typeof products !== 'undefined' && products && products.length > 0) { %>
          <div class="row g-4">
            <% products.forEach(function(product) { %>
              <div class="col-xl-3 col-lg-4 col-md-6">
                <div class="product-card h-100">
                  <div class="product-image-container">
                    <a href="/customer/product/<%= product._id %>" class="product-image-link">
                      <img src="<%= product.image || '/images/placeholder.png' %>" 
                           alt="<%= product.name %>" 
                           class="product-image" 
                           onerror="this.onerror=null; this.src='https://images.unsplash.com/photo-1517336714731-489689fd1ca8?w=500&auto=format&fit=crop&q=60';">
                    </a>
                    <span class="seizure-badge">
                      <i class="fas fa-gavel me-1"></i> Seized Asset
                    </span>
                    <span class="product-discount-tag"><%= product.condition || 'Verified' %></span>
                  </div>
                  <div class="product-content p-3 d-flex flex-column flex-grow-1">
                    <div class="d-flex justify-content-between align-items-center mb-1">
                      <span class="product-category">
                        <%= product.category ? (product.category.charAt(0).toUpperCase() + product.category.slice(1)) : 'Electronics' %>
                      </span>
                      <button class="btn-wishlist add-to-wishlist" title="Save to Wishlist" aria-label="Save to Wishlist">
                        <i class="far fa-heart"></i>
                      </button>
                    </div>

                    <h3 class="product-title">
                      <a href="/customer/product/<%= product._id %>">
                        <%= product.name %>
                      </a>
                    </h3>

                    <div class="agency-tag mb-3">
                      <i class="fas fa-landmark me-1"></i>
                      <span><%= product.agency && product.agency.name ? product.agency.name : 'Customs Disposal Authority' %></span>
                    </div>

                    <div class="product-footer mt-auto pt-2 border-top">
                      <div class="d-flex justify-content-between align-items-baseline mb-2">
                        <div class="product-price">
                          ₹<%= typeof product.price === 'number' ? product.price.toLocaleString('en-IN') : product.price %>
                        </div>
                        <span class="stock-status-badge">
                          <i class="fas fa-check-circle me-1"></i> Available
                        </span>
                      </div>

                      <div class="d-flex gap-2">
                        <button class="btn btn-add-cart add-to-cart add-to-cart-btn flex-grow-1"
                          data-id="<%= product._id %>"
                          data-name="<%= product.name %>"
                          data-price="<%= product.price %>"
                          data-image="<%= product.image %>">
                          <i class="fas fa-shopping-cart me-1"></i> Add to Cart
                        </button>
                        <a href="/customer/product/<%= product._id %>" class="btn btn-quick-view" title="View Details" aria-label="View Details">
                          <i class="fas fa-eye"></i>
                        </a>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            <% }); %>
          </div>
        <% } %>
      </div>
    </section>

    <!-- Trust & Value Highlights Strip -->
    <section class="trust-highlights-bar py-4">
      <div class="container">
        <div class="row g-4">
          <div class="col-lg-3 col-md-6">
            <div class="trust-item d-flex align-items-center gap-3">
              <div class="trust-icon-box">
                <i class="fas fa-scale-balanced"></i>
              </div>
              <div>
                <h6 class="trust-title mb-0">100% Legally Seized</h6>
                <p class="trust-desc mb-0">Confiscated by customs & courts</p>
              </div>
            </div>
          </div>
          <div class="col-lg-3 col-md-6">
            <div class="trust-item d-flex align-items-center gap-3">
              <div class="trust-icon-box">
                <i class="fas fa-lock"></i>
              </div>
              <div>
                <h6 class="trust-title mb-0">Escrow Protected</h6>
                <p class="trust-desc mb-0">Funds secured until verification</p>
              </div>
            </div>
          </div>
          <div class="col-lg-3 col-md-6">
            <div class="trust-item d-flex align-items-center gap-3">
              <div class="trust-icon-box">
                <i class="fas fa-tag"></i>
              </div>
              <div>
                <h6 class="trust-title mb-0">Zero Auction Markup</h6>
                <p class="trust-desc mb-0">Direct fixed clearance rates</p>
              </div>
            </div>
          </div>
          <div class="col-lg-3 col-md-6">
            <div class="trust-item d-flex align-items-center gap-3">
              <div class="trust-icon-box">
                <i class="fas fa-truck-fast"></i>
              </div>
              <div>
                <h6 class="trust-title mb-0">Inspected Delivery</h6>
                <p class="trust-desc mb-0">Free express insured courier</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- How SETU Works Timeline Section -->
    <section class="process-section py-5 bg-white border-top border-bottom">
      <div class="container py-3">
        <div class="text-center mb-5" style="max-width: 660px; margin: 0 auto;">
          <div class="process-badge mb-2">Transparency Process</div>
          <h2 class="process-heading">How Seized Goods Reach Citizens</h2>
          <p class="process-subheading">A transparent, government-compliant bridge ensuring verified authentic goods, fixed clearance pricing, and buyer safety.</p>
        </div>

        <div class="row g-4 text-center position-relative">
          <div class="col-md-4">
            <div class="process-card h-100">
              <span class="process-step-num">01</span>
              <div class="process-icon-wrapper mx-auto mb-3">
                <i class="fas fa-gavel"></i>
              </div>
              <h5 class="process-card-title mb-2">1. Official Confiscation</h5>
              <p class="process-card-desc mb-0">High-value electronics confiscated during airport customs checks, tax raids, and court bankruptcy liquidations.</p>
            </div>
          </div>
          <div class="col-md-4">
            <div class="process-card h-100">
              <span class="process-step-num">02</span>
              <div class="process-icon-wrapper mx-auto mb-3">
                <i class="fas fa-clipboard-check"></i>
              </div>
              <h5 class="process-card-title mb-2">2. Verification & Testing</h5>
              <p class="process-card-desc mb-0">Disposal authorities inspect hardware, check serial numbers against police records, and issue clearance certificates.</p>
            </div>
          </div>
          <div class="col-md-4">
            <div class="process-card h-100">
              <span class="process-step-num">03</span>
              <div class="process-icon-wrapper mx-auto mb-3">
                <i class="fas fa-shield-alt"></i>
              </div>
              <h5 class="process-card-title mb-2">3. Escrow Protected Resale</h5>
              <p class="process-card-desc mb-0">Citizens buy items directly without auction markups. Funds remain in escrow until you inspect your item upon delivery.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Portals Access Section -->
    <section class="portal-access-section py-5">
      <div class="container">
        <div class="text-center mb-4">
          <h2 class="portal-section-title">Platform Portals & Account Access</h2>
          <p class="text-muted small">Choose your access portal to manage purchases or inventory</p>
        </div>
        <div class="row g-4 justify-content-center">
          <div class="col-md-5">
            <div class="portal-box citizen-portal-box h-100">
              <div class="portal-icon-circle citizen-icon mx-auto mb-3">
                <i class="fas fa-user-circle"></i>
              </div>
              <h4 class="portal-box-title mb-2">Citizen & Buyer Portal</h4>
              <p class="portal-box-desc mb-4">Browse verified seized inventory, track live orders, manage escrow deliveries, and submit inspection reviews.</p>
              <div class="d-flex gap-2 justify-content-center flex-wrap">
                <a href="/auth/customer/login" class="btn btn-portal-primary">Sign In</a>
                <a href="/customer/register" class="btn btn-portal-outline">Create Account</a>
              </div>
            </div>
          </div>

          <div class="col-md-5">
            <div class="portal-box agency-portal-box h-100">
              <div class="portal-icon-circle agency-icon mx-auto mb-3">
                <i class="fas fa-landmark"></i>
              </div>
              <h4 class="portal-box-title mb-2">Government Agency Portal</h4>
              <p class="portal-box-desc mb-4">Upload disposal manifests, manage seized inventory batches, track lot clearance, and review fulfillment metrics.</p>
              <div class="d-flex gap-2 justify-content-center flex-wrap">
                <a href="/auth/agency/login" class="btn btn-portal-dark">Agency Sign In</a>
                <a href="/agency/register" class="btn btn-portal-outline">Register Agency</a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  <% } %>

  <!-- Customer Login Page -->
  <% if(page === 'customer-login') { %>
    <div class="auth-shell">
      <div class="auth-visual">
        <span class="auth-badge"><i class="fas fa-shopping-basket"></i> Customer Portal</span>
        <h1>Welcome back to SETU.</h1>
        <p>Discover curated products, track orders, and manage your account from one seamless shopping experience.</p>
        <ul class="auth-features">
          <li><i class="fas fa-check-circle"></i> Personalized recommendations</li>
          <li><i class="fas fa-check-circle"></i> Fast order tracking</li>
          <li><i class="fas fa-check-circle"></i> Secure shopping experience</li>
        </ul>
      </div>

      <div class="auth-panel">
        <div class="auth-panel-header">
          <a href="/" class="auth-logo-mark">
            <span class="logo-icon"><i class="fas fa-shopping-bag"></i></span>
            <span>SETU</span>
          </a>
        </div>

        <div class="auth-form-header">
          <h2>Customer Login</h2>
          <p>Welcome back! Please enter your details.</p>
        </div>

        <form action="/auth/customer/login" method="POST" class="auth-form">
          <div class="form-group">
            <label for="email">Email</label>
            <input id="email" name="email" type="email" placeholder="Enter your email" required>
          </div>

          <div class="form-group">
            <div class="auth-inline-row" style="align-items:end;">
              <label for="password">Password</label>
              <a href="/auth/customer/forgot-password" class="form-link" style="font-size:0.75rem; text-transform:uppercase; letter-spacing:0.05em;">Forgot password?</a>
            </div>
            <input id="password" name="password" type="password" placeholder="Enter your password" required>
          </div>

          <label class="auth-remember" for="remember">
            <input type="checkbox" id="remember" name="remember">
            <span>Remember me for 30 days</span>
          </label>

          <button type="submit" class="btn btn-primary">Sign in</button>

          <button type="button" class="btn btn-outline-secondary btn-sm rounded-pill w-100 mt-2" onclick="document.getElementById('email').value='customer@example.com'; document.getElementById('password').value='password123';">
            <i class="fas fa-magic me-1"></i> Fill Demo Customer Credentials
          </button>
        </form>

        <div class="auth-footer">
          <p>Don't have an account? <a href="/auth/customer/register">Sign up</a></p>
        </div>
      </div>
    </div>
  <% } %>

  <!-- Customer Register Page -->
  <% if(page === 'customer-register') { %>
    <div class="auth-shell">
      <div class="auth-visual">
        <span class="auth-badge"><i class="fas fa-user-plus"></i> Citizen Account</span>
        <h1>Start with SETU.</h1>
        <p>Direct citizen access to 100% verified seized assets, transparent clearance pricing, and escrow-protected checkout.</p>
        <ul class="auth-features">
          <li><i class="fas fa-shield-halved"></i> 100% Legally Verified Confiscations</li>
          <li><i class="fas fa-lock"></i> Escrow-Protected Payments</li>
          <li><i class="fas fa-truck-fast"></i> Inspected Express Delivery</li>
        </ul>
      </div>

      <div class="auth-panel">
        <div class="auth-panel-header">
          <a href="/" class="auth-logo-mark">
            <span class="logo-icon"><i class="fas fa-shield-halved"></i></span>
            <span>SETU</span>
          </a>
        </div>

        <div class="auth-form-header">
          <h2>Create Citizen Account</h2>
          <p>Register to purchase seized goods at fixed clearance rates.</p>
        </div>

        <% if(typeof errors !== 'undefined' && errors && errors.length > 0) { %>
          <% errors.forEach(function(error) { %>
            <div class="alert alert-danger py-2 px-3 small rounded-3 mb-2">
              <i class="fas fa-circle-exclamation me-1"></i> <%= error.msg %>
            </div>
          <% }); %>
        <% } %>

        <form action="/auth/customer/register" method="POST" class="auth-form">
          <div class="form-group">
            <label for="name">Full Name</label>
            <input id="name" name="name" type="text" placeholder="Enter your full name" required value="<%= typeof name != 'undefined' ? name : '' %>">
          </div>

          <div class="form-group">
            <label for="email">Email Address</label>
            <input id="email" name="email" type="email" placeholder="name@example.com" required value="<%= typeof email != 'undefined' ? email : '' %>">
          </div>

          <div class="form-row">
            <div class="form-group">
              <label for="password">Password</label>
              <input id="password" name="password" type="password" placeholder="Create password" required>
            </div>

            <div class="form-group">
              <label for="confirmPassword">Confirm Password</label>
              <input id="confirmPassword" name="confirmPassword" type="password" placeholder="Confirm password" required>
            </div>
          </div>

          <label class="auth-remember" for="terms">
            <input type="checkbox" id="terms" name="terms" required checked>
            <span>I agree to the <a href="/terms" class="text-primary fw-semibold">Terms of Service</a> & <a href="/privacy" class="text-primary fw-semibold">Privacy Policy</a></span>
          </label>

          <button type="submit" class="btn btn-primary w-100 py-2 fw-bold rounded-pill">
            <i class="fas fa-user-plus me-1"></i> Create Account
          </button>
        </form>

        <div class="auth-footer mt-4 text-center">
          <p class="text-muted small mb-0">Already have an account? <a href="/auth/customer/login" class="text-primary fw-bold text-decoration-none">Sign In &rarr;</a></p>
        </div>
      </div>
    </div>
  <% } %>

  <!-- Agency Login Page -->
  <% if(page === 'agency-login') { %>
    <div class="auth-shell">
      <div class="auth-visual">
        <span class="auth-badge"><i class="fas fa-building"></i> Agency Portal</span>
        <h1>Grow your business on SETU.</h1>
        <p>Manage your listings, track orders, and expand your digital storefront with a dedicated agency dashboard.</p>
        <ul class="auth-features">
          <li><i class="fas fa-check-circle"></i> Inventory management</li>
          <li><i class="fas fa-check-circle"></i> Order visibility</li>
          <li><i class="fas fa-check-circle"></i> Growth insights</li>
        </ul>
      </div>

      <div class="auth-panel">
        <div class="auth-panel-header">
          <a href="/" class="auth-logo-mark">
            <span class="logo-icon"><i class="fas fa-building"></i></span>
            <span>SETU</span>
          </a>
        </div>

        <div class="auth-form-header">
          <h2>Agency Login</h2>
          <p>Sign in to your agency account.</p>
        </div>

        <form action="/auth/agency/login" method="POST" class="auth-form">
          <div class="form-group">
            <label for="email">Email</label>
            <input id="email" name="email" type="email" placeholder="Enter your email" required>
          </div>

          <div class="form-group">
            <div class="auth-inline-row" style="align-items:end;">
              <label for="password">Password</label>
              <a href="/auth/agency/forgot-password" class="form-link" style="font-size:0.75rem; text-transform:uppercase; letter-spacing:0.05em;">Forgot password?</a>
            </div>
            <input id="password" name="password" type="password" placeholder="Enter your password" required>
          </div>

          <label class="auth-remember" for="remember">
            <input type="checkbox" id="remember" name="remember">
            <span>Remember me for 30 days</span>
          </label>

          <button type="submit" class="btn btn-primary">Sign in</button>

          <button type="button" class="btn btn-outline-secondary btn-sm rounded-pill w-100 mt-2" onclick="document.getElementById('email').value='agency@example.com'; document.getElementById('password').value='password123';">
            <i class="fas fa-magic me-1"></i> Fill Demo Agency Credentials
          </button>
        </form>

        <div class="auth-footer">
          <p>Don't have an agency account? <a href="/auth/agency/register">Apply now</a></p>
        </div>
      </div>
    </div>
  <% } %>

  <!-- Agency Register Page -->
  <% if(page === 'agency-register') { %>
    <div class="auth-shell" style="grid-template-columns: 1fr; max-width: 960px;">
      <div class="auth-panel" style="padding:2rem;">
        <div class="auth-panel-header">
          <a href="/" class="auth-logo-mark">
            <span class="logo-icon"><i class="fas fa-building"></i></span>
            <span>SETU</span>
          </a>
        </div>

        <div class="auth-form-header">
          <h2>Agency Registration</h2>
          <p>Create an agency account to start selling on our platform.</p>
        </div>

        <form action="/auth/agency/register" method="POST" class="auth-form">
          <div class="auth-section">
            <h3>Agency Information</h3>
            <div class="form-row">
              <div class="form-group">
                <label for="agencyName">Agency Name</label>
                <input id="agencyName" name="agencyName" type="text" placeholder="Enter your agency name" required>
              </div>
              <div class="form-group">
                <label for="businessType">Business Type</label>
                <select id="businessType" name="businessType" required>
                  <option value="">Select business type</option>
                  <option value="sole_proprietorship">Sole Proprietorship</option>
                  <option value="partnership">Partnership</option>
                  <option value="llc">Limited Liability Company (LLC)</option>
                  <option value="corporation">Corporation</option>
                  <option value="other">Other</option>
                </select>
              </div>
            </div>

            <div class="form-group">
              <label for="businessDescription">Business Description</label>
              <textarea id="businessDescription" name="businessDescription" rows="3" placeholder="Briefly describe your business and the products you sell" required></textarea>
            </div>

            <div class="form-row">
              <div class="form-group">
                <label for="website">Website (Optional)</label>
                <input id="website" name="website" type="url" placeholder="https://www.example.com">
              </div>
              <div class="form-group">
                <label for="phone">Business Phone</label>
                <input id="phone" name="phone" type="tel" placeholder="Enter business phone number" required>
              </div>
            </div>
          </div>

          <div class="auth-section">
            <h3>Contact Information</h3>
            <div class="form-row">
              <div class="form-group">
                <label for="firstName">First Name</label>
                <input id="firstName" name="firstName" type="text" placeholder="Enter your first name" required>
              </div>
              <div class="form-group">
                <label for="lastName">Last Name</label>
                <input id="lastName" name="lastName" type="text" placeholder="Enter your last name" required>
              </div>
            </div>

            <div class="form-row">
              <div class="form-group">
                <label for="email">Email Address</label>
                <input id="email" name="email" type="email" placeholder="Enter your email" required>
              </div>
              <div class="form-group">
                <label for="position">Position / Title</label>
                <input id="position" name="position" type="text" placeholder="Enter your position" required>
              </div>
            </div>
          </div>

          <div class="auth-section">
            <h3>Account Information</h3>
            <div class="form-row">
              <div class="form-group">
                <label for="password">Password</label>
                <input id="password" name="password" type="password" placeholder="Create a password" required>
              </div>
              <div class="form-group">
                <label for="confirmPassword">Confirm Password</label>
                <input id="confirmPassword" name="confirmPassword" type="password" placeholder="Confirm your password" required>
              </div>
            </div>
          </div>

          <label class="auth-remember" for="terms">
            <input type="checkbox" id="terms" name="terms" required>
            <span>I agree to the <a href="/terms">Terms of Service</a> and <a href="/privacy">Privacy Policy</a></span>
          </label>

          <label class="auth-remember" for="marketing">
            <input type="checkbox" id="marketing" name="marketing">
            <span>I agree to receive marketing communications from SETU</span>
          </label>

          <button type="submit" class="btn btn-primary">Submit Application</button>
        </form>

        <div class="auth-footer">
          <p>Already have an agency account? <a href="/auth/agency/login">Sign in</a></p>
        </div>
      </div>
    </div>
  <% } %>

  <!-- Customer Browsing Page -->
  <% if(page === 'customer-browsing') { %>
    <!-- Catalog Hero Banner -->
    <section class="py-4 border-bottom position-relative" style="background: linear-gradient(135deg, #0b1120 0%, #0f172a 50%, #0369a1 100%); color: #ffffff;">
      <div class="container py-2" style="max-width: 1320px;">
        <div class="row align-items-center g-3">
          <div class="col-lg-7">
            <div class="d-inline-flex align-items-center gap-2 px-3 py-1 rounded-pill mb-2" style="background: rgba(2, 132, 199, 0.25); border: 1px solid rgba(56, 189, 248, 0.35);">
              <i class="fas fa-gavel text-warning"></i>
              <span class="small fw-bold text-info text-uppercase" style="letter-spacing: 0.05em;">Direct Citizen Disposal Catalog</span>
            </div>
            <h1 class="h2 fw-extrabold text-white mb-2" style="font-weight: 800; letter-spacing: -0.02em;">
              Government Seized & Confiscated Assets
            </h1>
            <p class="text-white-50 mb-0 small" style="max-width: 580px;">
              Direct procurement of verified confiscations from Customs, Enforcement Directorate, and Police clearances. Inspected serial numbers, fixed clearance pricing, and 100% escrow protection.
            </p>
          </div>
          <div class="col-lg-5">
            <form action="/customer/search" method="POST" class="d-flex shadow-sm rounded-pill p-1 bg-white">
              <input type="text" name="searchTerm" placeholder="Search laptops, phones, cameras..." 
                     class="form-control border-0 rounded-pill ps-3 shadow-none" 
                     value="<%= locals.searchTerm || '' %>" required>
              <button type="submit" class="btn btn-primary rounded-pill px-4 fw-semibold">
                <i class="fas fa-search me-1"></i> Search
              </button>
            </form>
            <div class="d-flex gap-3 justify-content-end mt-2 text-white-50 small">
              <span><i class="fas fa-shield-alt text-success me-1"></i> Escrow Secured</span>
              <span><i class="fas fa-check-circle text-info me-1"></i> 100% Authentic</span>
              <span><i class="fas fa-truck text-warning me-1"></i> Fast Shipping</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Catalog Content -->
    <div class="container py-4" style="max-width: 1320px;">
      <!-- Breadcrumb & Stats Bar -->
      <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap gap-2">
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb mb-0 bg-transparent p-0 small">
            <li class="breadcrumb-item"><a href="/" class="text-decoration-none text-muted"><i class="fas fa-home me-1"></i> Home</a></li>
            <li class="breadcrumb-item"><a href="/customer/browsing" class="text-decoration-none text-muted">Catalog</a></li>
            <li class="breadcrumb-item active text-dark fw-bold text-capitalize" aria-current="page">
              <%= (typeof activeTab !== 'undefined' && activeTab !== 'all') ? activeTab : 'All Products' %>
            </li>
          </ol>
        </nav>
        <div class="badge bg-light text-dark border px-3 py-2 rounded-pill fw-semibold shadow-xs">
          <i class="fas fa-box text-primary me-1"></i> Showing <%= (typeof products !== 'undefined' && products) ? products.length : 0 %> Seized Assets
        </div>
      </div>

      <!-- Category Filter Pills -->
      <div class="d-flex flex-wrap gap-2 mb-4 pb-2 border-bottom">
        <a href="/customer/browsing" 
           class="btn rounded-pill px-4 py-2 fw-semibold <%= (activeTab === 'all' || !activeTab) ? 'btn-primary shadow-sm' : 'btn-outline-secondary' %>">
          <i class="fas fa-th-large me-1"></i> All Items
        </a>
        <a href="/customer/browsing/laptops" 
           class="btn rounded-pill px-4 py-2 fw-semibold <%= activeTab === 'laptops' ? 'btn-primary shadow-sm' : 'btn-outline-secondary' %>">
          <i class="fas fa-laptop me-1"></i> Laptops & Computers
        </a>
        <a href="/customer/browsing/mobile" 
           class="btn rounded-pill px-4 py-2 fw-semibold <%= activeTab === 'mobile' ? 'btn-primary shadow-sm' : 'btn-outline-secondary' %>">
          <i class="fas fa-mobile-alt me-1"></i> Smartphones
        </a>
        <a href="/customer/browsing/cameras" 
           class="btn rounded-pill px-4 py-2 fw-semibold <%= activeTab === 'cameras' ? 'btn-primary shadow-sm' : 'btn-outline-secondary' %>">
          <i class="fas fa-camera me-1"></i> Cameras & Drones
        </a>
        <a href="/customer/browsing/electronics" 
           class="btn rounded-pill px-4 py-2 fw-semibold <%= activeTab === 'electronics' ? 'btn-primary shadow-sm' : 'btn-outline-secondary' %>">
          <i class="fas fa-headphones me-1"></i> Audio & Electronics
        </a>
      </div>

      <!-- Product Cards Grid -->
      <% if(typeof products !== 'undefined' && products && products.length > 0) { %>
        <div class="row g-4">
          <% products.forEach(function(product) { %>
            <div class="col-xl-3 col-lg-4 col-md-6">
              <div class="product-card h-100 shadow-sm border-0 d-flex flex-column" style="border-radius: 18px; overflow: hidden; background: #ffffff; transition: transform 0.2s, box-shadow 0.2s;">
                <div class="product-image-container position-relative overflow-hidden" style="height: 200px; background: #f8fafc;">
                  <a href="/customer/product/<%= product._id %>" style="display:block; height:100%;">
                    <img src="<%= product.image || '/images/placeholder.png' %>" 
                         alt="<%= product.name %>" 
                         class="product-image w-100 h-100" 
                         style="object-fit: cover; transition: transform 0.35s ease;"
                         onerror="this.onerror=null; this.src='https://via.placeholder.com/300x200?text=Seized+Product';">
                  </a>
                  <span class="position-absolute top-0 start-0 m-2 badge bg-dark text-warning rounded-pill px-2 py-1 shadow-sm small">
                    <i class="fas fa-gavel me-1"></i> Seized Asset
                  </span>
                  <span class="position-absolute top-0 end-0 m-2 badge bg-primary text-white rounded-pill px-2 py-1 shadow-sm small">
                    <%= product.condition || 'Verified' %>
                  </span>
                </div>

                <div class="p-3 d-flex flex-column flex-grow-1">
                  <div class="d-flex justify-content-between align-items-center mb-1">
                    <span class="product-category fw-bold small text-primary text-uppercase" style="font-size: 0.72rem; letter-spacing: 0.05em;">
                      <%= product.category ? (product.category.charAt(0).toUpperCase() + product.category.slice(1)) : 'Electronics' %>
                    </span>
                    <button class="btn btn-sm btn-icon add-to-wishlist border-0 bg-transparent text-muted" title="Save to Wishlist">
                      <i class="far fa-heart"></i>
                    </button>
                  </div>

                  <h3 class="h6 fw-bold mb-1" style="font-size: 0.95rem; line-height: 1.35;">
                    <a href="/customer/product/<%= product._id %>" class="text-dark text-decoration-none">
                      <%= product.name %>
                    </a>
                  </h3>

                  <small class="text-muted d-block mb-3" style="font-size: 0.78rem;">
                    <i class="fas fa-landmark me-1 text-secondary"></i>
                    <%= product.agency && product.agency.name ? product.agency.name : 'Customs Disposal Authority' %>
                  </small>

                  <div class="mt-auto pt-2 border-top">
                    <div class="d-flex justify-content-between align-items-baseline mb-2">
                      <div class="h5 fw-bold text-primary mb-0">
                        ₹<%= typeof product.price === 'number' ? product.price.toLocaleString('en-IN') : product.price %>
                      </div>
                      <span class="badge bg-light text-success border small">
                        <i class="fas fa-check-circle me-1"></i> In Stock (<%= product.stock || 1 %>)
                      </span>
                    </div>

                    <div class="d-flex gap-2">
                      <button class="btn btn-primary add-to-cart add-to-cart-btn flex-grow-1 rounded-pill fw-semibold py-2"
                        data-id="<%= product._id %>"
                        data-name="<%= product.name %>"
                        data-price="<%= product.price %>"
                        data-image="<%= product.image %>">
                        <i class="fas fa-shopping-cart me-1"></i> Add to Cart
                      </button>
                      <a href="/customer/product/<%= product._id %>" class="btn btn-outline-secondary rounded-circle d-flex align-items-center justify-content-center" style="width: 38px; height: 38px;" title="View Details">
                        <i class="fas fa-eye"></i>
                      </a>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          <% }); %>
        </div>
      <% } else { %>
        <div class="py-5 text-center bg-white rounded-4 border p-5 shadow-sm my-4">
          <div class="rounded-circle bg-light text-muted d-flex align-items-center justify-content-center mx-auto mb-3" style="width: 70px; height: 70px; font-size: 2rem;">
            <i class="fas fa-box-open"></i>
          </div>
          <h4 class="fw-bold mb-1">No Seized Products Found</h4>
          <p class="text-muted mb-4" style="max-width: 440px; margin: 0 auto;">
            No confiscated inventory matches your selection right now. More seized lots are cleared every week.
          </p>
          <a href="/customer/browsing" class="btn btn-primary rounded-pill px-4 fw-semibold">
            <i class="fas fa-store me-1"></i> Browse All Confiscations
          </a>
        </div>
      <% } %>
    </div>
  <% } %>

  <!-- Customer Product Details Page -->
  <% if(page === 'customer-product-details' && typeof product !== 'undefined' && product) { %>
    <div class="container my-4" style="max-width: 1200px;">
      <!-- Breadcrumb -->
      <nav aria-label="breadcrumb" class="mb-4">
        <ol class="breadcrumb bg-white p-3 rounded-pill shadow-sm border">
          <li class="breadcrumb-item"><a href="/" class="text-decoration-none text-muted"><i class="fas fa-home me-1"></i> Home</a></li>
          <li class="breadcrumb-item"><a href="/customer/browsing" class="text-decoration-none text-muted">Browse Catalog</a></li>
          <li class="breadcrumb-item"><a href="/customer/browsing/<%= product.category %>" class="text-decoration-none text-muted text-capitalize"><%= product.category %></a></li>
          <li class="breadcrumb-item active text-dark fw-bold text-truncate" style="max-width: 280px;" aria-current="page"><%= product.name %></li>
        </ol>
      </nav>

      <div class="card border-0 shadow-sm rounded-4 overflow-hidden mb-5">
        <div class="row g-0">
          <!-- Product Image Column -->
          <div class="col-lg-6 p-4 p-md-5 bg-light d-flex flex-column align-items-center justify-content-center border-end">
            <div class="position-relative w-100 text-center" style="max-height: 440px;">
              <img src="<%= product.image || '/images/placeholder.png' %>" 
                   alt="<%= product.name %>" 
                   class="img-fluid rounded-4 shadow-sm" 
                   style="max-height: 420px; object-fit: contain; width: 100%;"
                   onerror="this.onerror=null; this.src='https://via.placeholder.com/500x400?text=Product+Image';">
              <span class="position-absolute top-0 start-0 badge bg-dark text-white px-3 py-2 rounded-pill shadow-sm">
                <i class="fas fa-shield-alt text-warning me-1"></i> Seized & Verified
              </span>
            </div>
          </div>

          <!-- Product Details Column -->
          <div class="col-lg-6 p-4 p-md-5 d-flex flex-column justify-content-between">
            <div>
              <div class="d-flex align-items-center gap-2 mb-2">
                <span class="badge bg-primary-subtle text-primary px-3 py-1 rounded-pill fw-bold text-uppercase" style="background:#e0f2fe; color:#0369a1;">
                  <%= product.category %>
                </span>
                <span class="badge bg-success-subtle text-success px-3 py-1 rounded-pill fw-bold" style="background:#dcfce7; color:#15803d;">
                  Condition: <%= product.condition || 'Refurbished A+' %>
                </span>
              </div>

              <h1 class="h2 fw-bold text-dark mb-3"><%= product.name %></h1>

              <div class="p-3 bg-light rounded-3 border mb-4">
                <div class="d-flex align-items-center gap-2 mb-1">
                  <i class="fas fa-landmark text-primary"></i>
                  <span class="fw-bold text-dark">Disposal Agency:</span>
                  <span><%= product.agency && product.agency.name ? product.agency.name : 'Government Custody Division' %></span>
                </div>
                <div class="d-flex align-items-center gap-2">
                  <i class="fas fa-check-circle text-success"></i>
                  <small class="text-muted">Legal seizure clearance manifest verified. Escrow purchase protection guaranteed.</small>
                </div>
              </div>

              <div class="mb-4">
                <div class="text-muted small text-uppercase fw-bold mb-1">Fixed Clearance Price</div>
                <div class="d-flex align-items-baseline gap-3">
                  <span class="h1 fw-bold text-primary mb-0">₹<%= typeof product.price === 'number' ? product.price.toLocaleString('en-IN') : product.price %></span>
                  <span class="text-muted text-decoration-line-through">₹<%= typeof product.price === 'number' ? Math.round(product.price * 1.45).toLocaleString('en-IN') : '' %></span>
                  <span class="badge bg-danger rounded-pill px-2 py-1">Save 31%</span>
                </div>
              </div>

              <p class="text-secondary leading-relaxed mb-4">
                <%= product.description || 'Verified seized electronic unit inspected and cleared for direct public resale under official disposal regulations.' %>
              </p>
            </div>

            <!-- Action Buttons -->
            <div>
              <div class="d-flex align-items-center gap-3 mb-4">
                <div class="d-flex align-items-center border rounded-pill px-3 py-1 bg-white">
                  <span class="text-muted small me-2">Quantity:</span>
                  <input type="number" data-product-id="<%= product._id %>" value="1" min="1" max="<%= product.stock || 10 %>" 
                         class="form-control form-control-sm border-0 text-center fw-bold" style="width: 50px;">
                </div>
                <span class="text-muted small">
                  <i class="fas fa-box text-primary me-1"></i> Stock Available: <strong><%= product.stock || 1 %></strong>
                </span>
              </div>

              <div class="d-flex gap-3">
                <button class="btn btn-primary add-to-cart add-to-cart-btn rounded-pill px-4 py-3 fw-bold flex-grow-1 shadow-sm"
                  data-id="<%= product._id %>"
                  data-name="<%= product.name %>"
                  data-price="<%= product.price %>"
                  data-image="<%= product.image %>">
                  <i class="fas fa-shopping-cart me-2"></i> Add to Cart
                </button>
                <a href="/customer/checkout" class="btn btn-dark rounded-pill px-4 py-3 fw-bold add-to-cart add-to-cart-btn"
                  data-id="<%= product._id %>"
                  data-name="<%= product.name %>"
                  data-price="<%= product.price %>"
                  data-image="<%= product.image %>">
                  <i class="fas fa-bolt me-2 text-warning"></i> Buy Now
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% } %>

  <!-- Customer Cart Page -->
  <% if(page === 'customer-cart') { %>
    <div class="container cart-page-wrapper">
      <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap gap-2">
        <div>
          <h1 class="h2 fw-bold mb-1"><i class="fas fa-shopping-bag text-primary me-2"></i> Shopping Cart</h1>
          <p class="text-muted mb-0">Review your selected seized items and proceed to secure checkout</p>
        </div>
        <a href="/customer/browsing" class="btn btn-outline-primary rounded-pill px-4 btn-sm fw-semibold">
          <i class="fas fa-arrow-left me-1"></i> Continue Shopping
        </a>
      </div>

      <% if (typeof cart !== 'undefined' && cart && cart.length > 0) { %>
        <div class="row g-4">
          <!-- Items List Column -->
          <div class="col-lg-8">
            <div class="card border-0 shadow-sm rounded-4 overflow-hidden">
              <div class="card-header bg-white py-3 px-4 border-bottom">
                <h5 class="fw-bold mb-0">Cart Items (<%= cart.length %>)</h5>
              </div>
              <div class="card-body p-4">
                <div class="cart-items" id="cartItems">
                  <% cart.forEach(item => { %>
                    <div class="cart-item-row" data-product-id="<%= item.productId %>" data-price="<%= item.price %>">
                      <img src="<%= item.image || '/images/placeholder.png' %>" 
                           alt="<%= item.name %>" 
                           class="cart-item-thumb" 
                           onerror="this.onerror=null; this.src='https://via.placeholder.com/80x80?text=Item';">
                      
                      <div class="flex-grow-1">
                        <h6 class="fw-bold text-dark mb-1"><%= item.name %></h6>
                        <small class="text-muted d-block mb-1">Verified Seized Asset</small>
                        <div class="text-primary fw-semibold">
                          ₹<%= (item.price || 0).toLocaleString('en-IN') %> per unit
                        </div>
                      </div>

                      <!-- Quantity Stepper -->
                      <div class="cart-qty-ctrl">
                        <button type="button" class="cart-qty-btn cart-qty-minus">-</button>
                        <input type="text" class="cart-qty-input" value="<%= item.quantity %>" readonly>
                        <button type="button" class="cart-qty-btn cart-qty-plus">+</button>
                      </div>

                      <!-- Subtotal for item -->
                      <div class="fw-bold text-dark fs-6 cart-item-subtotal text-end" style="min-width: 90px;">
                        ₹<%= ((item.price || 0) * (item.quantity || 1)).toLocaleString('en-IN') %>
                      </div>

                      <!-- Remove Button -->
                      <button type="button" class="cart-item-remove-btn" title="Remove item">
                        <i class="fas fa-trash-alt"></i>
                      </button>
                    </div>
                  <% }) %>
                </div>
              </div>
            </div>

            <!-- Escrow Notice -->
            <div class="p-3 bg-light rounded-4 border mt-3 d-flex align-items-center gap-3">
              <i class="fas fa-shield-halved text-success fa-2x"></i>
              <div>
                <strong class="d-block text-dark">Protected by SETU Escrow</strong>
                <small class="text-muted">Your funds are safe. Payment is only released to the agency after your 7-day inspection window.</small>
              </div>
            </div>
          </div>

          <!-- Order Summary Column -->
          <div class="col-lg-4">
            <div class="card border-0 shadow-sm rounded-4 overflow-hidden sticky-top" style="top: 5.5rem;">
              <div class="card-header bg-white py-3 px-4 border-bottom">
                <h5 class="fw-bold mb-0">Order Summary</h5>
              </div>
              <div class="card-body p-4">
                <div class="d-flex justify-content-between mb-2">
                  <span class="text-muted">Subtotal</span>
                  <span class="fw-bold text-dark" id="cartSubtotal">₹<%= (subtotal || 0).toLocaleString('en-IN') %></span>
                </div>

                <div class="d-flex justify-content-between mb-2">
                  <span class="text-muted">Estimated GST (10%)</span>
                  <span class="fw-bold text-dark" id="cartTax">₹<%= (tax || 0).toFixed(2) %></span>
                </div>

                <div class="d-flex justify-content-between mb-3">
                  <span class="text-muted">Delivery Charges</span>
                  <span class="text-success fw-bold" id="cartShipping">₹5.00</span>
                </div>

                <hr class="my-3">

                <div class="d-flex justify-content-between align-items-baseline mb-4">
                  <span class="h6 fw-bold mb-0">Total Amount</span>
                  <span class="h4 fw-bold text-primary mb-0" id="cartTotal">₹<%= Math.round(total || 0).toLocaleString('en-IN') %></span>
                </div>

                <a href="/customer/checkout" class="btn btn-primary rounded-pill w-100 py-3 fw-bold fs-6 shadow-sm d-flex align-items-center justify-content-center gap-2" id="checkoutBtn">
                  <span>Proceed to Checkout</span>
                  <i class="fas fa-arrow-right"></i>
                </a>

                <div class="mt-4 pt-3 border-top">
                  <div class="input-group">
                    <input type="text" class="form-control rounded-start-pill ps-3" placeholder="Promo code (e.g. SETU10)" id="promoInput">
                    <button class="btn btn-outline-secondary rounded-end-pill px-3" type="button" onclick="showToast('Promo code SETU10 applied! 10% discount', 'success')">Apply</button>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      <% } else { %>
        <div class="card border-0 shadow-sm rounded-4 p-5 text-center my-4">
          <div class="text-primary mb-3 opacity-50">
            <i class="fas fa-shopping-cart fa-4x"></i>
          </div>
          <h3 class="fw-bold mb-2">Your Shopping Cart is Empty</h3>
          <p class="text-muted mb-4">You haven't added any seized electronic items to your cart yet.</p>
          <div>
            <a href="/customer/browsing" class="btn btn-primary rounded-pill px-5 py-2 fw-semibold shadow-sm">
              <i class="fas fa-store me-2"></i> Explore Seized Catalog
            </a>
          </div>
        </div>
      <% } %>
    </div>
  <% } %>

  <!-- Customer Checkout Page -->
  <% if(page === 'customer-checkout') { %>
    <div class="container">
      <!-- Mobile Sidebar Toggle -->
      <button class="mobile-sidebar-toggle" id="sidebarToggle">
        <i class="fas fa-bars"></i>
      </button>

      <!-- Mobile Header -->
      <div class="mobile-header">
        <div class="mobile-header-content">
          <a href="/customer/browsing" class="mobile-logo">SETU</a>
        </div>
      </div>

      <!-- Sidebar -->
      <%- include('partials/sidebar') %>

      <!-- Main Content -->
      <main class="main-content">
        <div class="page-header">
          <h1 class="page-title">Checkout</h1>
          <p class="page-description">Complete your purchase</p>
        </div>

        <form action="/customer/place-order" method="POST">
          <div class="flex-wrap">
            <div class="flex-1 min-w-300">
              <div class="card">
                <div class="card-header">
                  <h2 class="card-title">Shipping Information</h2>
                  <p class="card-description">Enter your shipping details</p>
                </div>
                <div class="card-content">
                  <div class="form-row">
                    <div class="form-group">
                      <label for="firstName" class="form-label">First Name</label>
                      <input type="text" id="firstName" name="firstName" class="form-input" required>
                    </div>
                    <div class="form-group">
                      <label for="lastName" class="form-label">Last Name</label>
                      <input type="text" id="lastName" name="lastName" class="form-input" required>
                    </div>
                  </div>

                  <div class="form-row">
                    <div class="form-group">
                      <label for="email" class="form-label">Email</label>
                      <input type="email" id="email" name="email" class="form-input" required>
                    </div>
                    <div class="form-group">
                      <label for="phone" class="form-label">Phone</label>
                      <input type="tel" id="phone" name="phone" class="form-input" required>
                    </div>
                  </div>

                  <div class="form-group">
                    <label for="address" class="form-label">Address</label>
                    <input type="text" id="address" name="address" class="form-input" required>
                  </div>

                  <div class="form-row">
                    <div class="form-group">
                      <label for="city" class="form-label">City</label>
                      <input type="text" id="city" name="city" class="form-input" required>
                    </div>
                    <div class="form-group">
                      <label for="state" class="form-label">State</label>
                      <input type="text" id="state" name="state" class="form-input" required>
                    </div>
                  </div>

                  <div class="form-row">
                    <div class="form-group">
                      <label for="zip" class="form-label">ZIP Code</label>
                      <input type="text" id="zip" name="zip" class="form-input" required>
                    </div>
                    <div class="form-group">
                      <label for="country" class="form-label">Country</label>
                      <input type="text" id="country" name="country" class="form-input" required>
                    </div>
                  </div>
                </div>
              </div>

              <div class="card">
                <div class="card-header">
                  <h2 class="card-title">Payment Method</h2>
                  <p class="card-description">Select your preferred payment method</p>
                </div>
                <div class="card-content">
                  <div class="payment-methods">
                    <div class="payment-method-tabs">
                      <div class="payment-method-tab active" data-method="credit-card">
                        <i class="far fa-credit-card"></i> Credit Card
                      </div>
                      <div class="payment-method-tab" data-method="paypal">
                        <i class="fab fa-paypal"></i> PayPal
                      </div>
                      <div class="payment-method-tab" data-method="apple-pay">
                        <i class="fab fa-apple-pay"></i> Apple Pay
                      </div>
                      <div class="payment-method-tab" data-method="paytm">
                        <i class="fas fa-wallet"></i> Paytm
                      </div>
                    </div>

                    <div class="payment-method-content active" id="credit-card-content">
                      <div class="credit-card-info">
                        <div class="form-group">
                          <label for="cardNumber" class="form-label">Card Number</label>
                          <input type="text" id="cardNumber" name="cardNumber" class="form-input" placeholder="Enter your card number">
                        </div>
                        <div class="form-group">
                          <label for="cardName" class="form-label">Name on Card</label>
                          <input type="text" id="cardName" name="cardName" class="form-input" placeholder="Enter the name on the card">
                        </div>
                        <div class="form-row">
                          <div class="form-group">
                            <label for="cardExpiry" class="form-label">Expiration Date</label>
                            <input type="text" id="cardExpiry" name="cardExpiry" class="form-input" placeholder="MM/YY">
                          </div>
                          <div class="form-group">
                            <label for="cardCvv" class="form-label">CVV</label>
                            <input type="text" id="cardCvv" name="cardCvv" class="form-input" placeholder="CVV">
                          </div>
                        </div>
                      </div>
                    </div>

                    <div class="payment-method-content" id="paypal-content">
                      <div class="paypal-info">
                        <p>You will be redirected to PayPal to complete your payment securely.</p>
                        <button type="button" class="btn btn-primary mt-4">Pay with PayPal</button>
                      </div>
                    </div>

                    <div class="payment-method-content" id="apple-pay-content">
                      <div class="apple-pay-info">
                        <p>Pay securely with Apple Pay.</p>
                        <button type="button" class="btn btn-primary mt-4">
                          <i class="fab fa-apple-pay"></i> Pay with Apple Pay
                        </button>
                      </div>
                    </div>

                    <div class="payment-method-content" id="paytm-content">
                      <div class="paytm-info">
                        <p>You will be redirected to Paytm to complete your payment securely.</p>
                        <div class="paytm-logo">
                          <div class="paytm-logo-image">
                            <i class="fas fa-wallet fa-2x"></i> <span class="paytm-text">Paytm</span>
                          </div>
                        </div>
                        <div class="paytm-form">
                          <div class="form-group">
                            <label for="paytmMobile" class="form-label">Mobile Number</label>
                            <input type="tel" id="paytmMobile" name="paytmMobile" class="form-input" placeholder="Enter your registered mobile number" maxlength="10">
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>
                  <input type="hidden" name="paymentMethod" id="paymentMethod" value="credit-card">
                </div>
              </div>
            </div>

            <div class="cart-sidebar">
              <div class="card cart-summary">
                <div class="card-header">
                  <h2 class="card-title">Order Summary</h2>
                </div>
                <div class="card-content">
                  <div class="checkout-items">
                    <% if(cart && cart.length > 0) { %>
                      <% cart.forEach(function(item) { %>
                        <div class="checkout-item">
                          <div class="checkout-item-image">
                            <img src="<%= item.image %>" alt="<%= item.name %>">
                          </div>
                          <div class="checkout-item-details">
                            <div class="checkout-item-name"><%= item.name %></div>
                            <div class="checkout-item-price">
                              $<%= item.price.toFixed(2) %> × <%= item.quantity %>
                            </div>
                          </div>
                        </div>
                      <% }); %>
                    <% } %>
                  </div>

                  <div class="cart-subtotal">
                    <span>Subtotal</span>
                    <span class="cart-subtotal-value">$<%= subtotal.toFixed(2) %></span>
                  </div>

                  <div class="cart-tax">
                    <span>Tax (10%)</span>
                    <span class="cart-tax-value">$<%= tax.toFixed(2) %></span>
                  </div>

                  <div class="cart-shipping">
                    <span>Shipping</span>
                    <span class="cart-shipping-value">$<%= shipping.toFixed(2) %></span>
                  </div>

                  <div class="cart-total">
                    <span>Total</span>
                    <span class="cart-total-value">$<%= total.toFixed(2) %></span>
                  </div>

                  <button type="submit" class="btn btn-primary btn-block">Place Order</button>
                </div>
              </div>

              <div class="card mt-4">
                <div class="card-header">
                  <h3 class="card-title">Secure Checkout</h3>
                </div>
                <div class="card-content">
                  <p class="secure-text">All your information is secured by 256-bit SSL encryption.</p>
                  <div class="payment-icons">
                    <i class="fab fa-cc-visa"></i>
                    <i class="fab fa-cc-mastercard"></i>
                    <i class="fab fa-cc-amex"></i>
                    <i class="fab fa-cc-discover"></i>
                    <i class="fab fa-cc-paypal"></i>
                    <i class="fab fa-apple-pay"></i>
                    <i class="fas fa-wallet paytm-icon"></i>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </form>
      </main>
    </div>
  <% } %>

  <!-- Include other page templates here -->

  <!-- Footer partials for other pages -->
  <% if(page !== 'home' && !page.includes('login') && !page.includes('register')) { %>
    <%- include('partials/footer') %>
  <% } %>
<!-- <script src="../public/js/customer.js"></script> -->

    <script>
  //     function validateForm() {
  //     const name = document.getElementById("name").value;
  //     const email = document.getElementById("email").value;
  //     const password = document.getElementById("password").value;

  //     // Name validation: Only letters and spaces allowed
  //     const namePattern = /^[A-Za-z\s]+$/;
  //     if (!namePattern.test(name)) {
  //         alert("Name should only contain letters.");
  //         return false;
  //     }

  //     // Email validation: Must end with "@gmail.com"
  //     if (!email.endsWith("@gmail.com")) {
  //         alert("Email must be a Gmail address (@gmail.com).");
  //         return false;
  //     }

  //     // Password validation: At least 8 characters and one special character
  //     const passwordPattern = /^(?=.*[\W_]).{8,}$/;
  //     if (!passwordPattern.test(password)) {
  //         alert("Password must be at least 8 characters long and include a special character.");
  //         return false;
  //     }

  //     alert("Registration successful!");
  //     return true;
  // }

  // function validateForm() {
  //     const name = document.getElementById("name").value;
  //     const email = document.getElementById("email").value;
  //     const password = document.getElementById("password").value;

  //     // Name validation: Only letters and spaces allowed
  //     const namePattern = /^[A-Za-z\s]+$/;
  //     if (!namePattern.test(name)) {
  //         alert("Name should only contain letters.");
  //         return false;
  //     }

  //     // Email validation: Must end with "@gmail.com"
  //     if (!email.endsWith("@gmail.com")) {
  //         alert("Email must be a Gmail address (@gmail.com).");
  //         return false;
  //     }

  //     // Password validation: At least 8 characters and one special character
  //     const passwordPattern = /^(?=.*[\W_]).{8,}$/;
  //     if (!passwordPattern.test(password)) {
  //         alert("Password must be at least 8 characters long and include a special character.");
  //         return false;
  //     }

  //     alert("Registration successful!");
  //     return true;
  //}

        // Sample data - would be replaced with API calls in a real implementation
        // const documents = [
        //     {
        //         id: 'DOC-001',
        //         agency: 'Travel Agency Ltd',
        //         type: 'Business License',
        //         date: '2025-03-20',
        //         status: 'pending',
        //         fileType: 'pdf',
        //         fileUrl: 'https://www.w3.org/WAI/ER/tests/xhtml/testfiles/resources/pdf/dummy.pdf',
        //         notes: ''
        //     },
        //     {
        //         id: 'DOC-002',
        //         agency: 'Holiday Experts',
        //         type: 'Tax Certificate',
        //         date: '2025-03-19',
        //         status: 'pending',
        //         fileType: 'image',
        //         fileUrl: 'https://via.placeholder.com/800x600?text=Tax+Certificate',
        //         notes: ''
        //     },
        //     {
        //         id: 'DOC-003',
        //         agency: 'Adventure Tours',
        //         type: 'Insurance Policy',
        //         date: '2025-03-18',
        //         status: 'pending',
        //         fileType: 'pdf',
        //         fileUrl: 'https://www.w3.org/WAI/ER/tests/xhtml/testfiles/resources/pdf/dummy.pdf',
        //         notes: ''
        //     },
        //     {
        //         id: 'DOC-004',
        //         agency: 'Global Vacations',
        //         type: 'Business License',
        //         date: '2025-03-15',
        //         status: 'approved',
        //         verifiedDate: '2025-03-16',
        //         fileType: 'image',
        //         fileUrl: 'https://via.placeholder.com/800x600?text=Business+License',
        //         notes: 'All documents appear to be in order. Business license is valid for 2 years.',
        //         verifiedBy: 'Admin User'
        //     },
        //     {
        //         id: 'DOC-005',
        //         agency: 'Dream Holidays',
        //         type: 'Tax Certificate',
        //         date: '2025-03-12',
        //         status: 'rejected',
        //         verifiedDate: '2025-03-13',
        //         fileType: 'pdf',
        //         fileUrl: 'https://www.w3.org/WAI/ER/tests/xhtml/testfiles/resources/pdf/dummy.pdf',
        //         notes: 'Tax certificate is expired. Please upload the most recent version.',
        //         verifiedBy: 'Admin User'
        //     }
        // ];

        // const agencyFunctions = [
        //     {
        //         id: 'FUNC-001',
        //         name: 'Automated Booking System',
        //         agency: 'Travel Agency Ltd',
        //         description: 'This function provides an automated booking system for travel packages. It integrates with payment gateways and sends confirmation emails to customers.',
        //         dateAdded: '2025-03-10',
        //         status: 'active',
        //         notes: ''
        //     },
        //     {
        //         id: 'FUNC-002',
        //         name: 'Customer Feedback Collection',
        //         agency: 'Holiday Experts',
        //         description: 'This module collects and analyzes customer feedback after trips, generating satisfaction reports and improvement suggestions.',
        //         dateAdded: '2025-03-15',
        //         status: 'pending',
        //         notes: ''
        //     },
        //     {
        //         id: 'FUNC-003',
        //         name: 'Dynamic Pricing Algorithm',
        //         agency: 'Adventure Tours',
        //         description: 'An algorithm that adjusts package prices based on demand, seasonality, and competitor pricing. Helps maximize bookings and revenue.',
        //         dateAdded: '2025-03-18',
        //         status: 'active',
        //         notes: 'Approved after testing with sample data'
        //     }
        // ];

        // // Initialize the page
        // document.addEventListener('DOMContentLoaded', function() {
        //     populatePendingDocuments();
        //     populateVerifiedDocuments();
        //     populateAgencyFunctions();

        //     // Close document modal when clicking the X
        //     document.querySelector('#documentModal .close').addEventListener('click', function() {
        //         document.getElementById('documentModal').style.display = 'none';
        //     });

        //     // Close modals when clicking outside of them
        //     window.addEventListener('click', function(event) {
        //         const docModal = document.getElementById('documentModal');
        //         const funcModal = document.getElementById('functionModal');

        //         if (event.target == docModal) {
        //             docModal.style.display = 'none';
        //         }
        //         if (event.target == funcModal) {
        //             funcModal.style.display = 'none';
        //         }
        //     });
        // });

        // // Tab switching function
        // function openTab(tabName) {
        //     // Hide all tab contents
        //     const tabContents = document.getElementsByClassName('tab-content');
        //     for (let i = 0; i < tabContents.length; i++) {
        //         tabContents[i].style.display = 'none';
        //     }

        //     // Remove active class from all tabs
        //     const tabs = document.getElementsByClassName('tab');
        //     for (let i = 0; i < tabs.length; i++) {
        //         tabs[i].classList.remove('active');
        //     }

        //     // Show the selected tab content and set it as active
        //     document.getElementById(tabName).style.display = 'block';
        //     document.querySelector(⁠ .tab[onclick="openTab('${tabName}')"] ⁠).classList.add('active');
        // }

        // // Populate tables with data
        // function populatePendingDocuments() {
        //     const tableBody = document.getElementById('pending-documents');
        //     tableBody.innerHTML = '';

        //     const pendingDocs = documents.filter(doc => doc.status === 'pending');

        //     pendingDocs.forEach(doc => {
        //         const row = document.createElement('tr');
        //         row.innerHTML = `
        //             <td>${doc.id}</td>
        //             <td>${doc.agency}</td>
        //             <td><i class="fas ${getDocumentIcon(doc.type)} document-type-icon"></i> ${doc.type}</td>
        //             <td>${doc.date}</td>
        //             <td><span class="badge badge-pending">Pending</span></td>
        //             <td>
        //                 <button class="btn btn-view" onclick="viewDocument('${doc.id}')">
        //                     <i class="fas fa-eye"></i> View
        //                 </button>
        //             </td>
        //         `;
        //         tableBody.appendChild(row);
        //     });
        // }

        // function populateVerifiedDocuments() {
        //     const tableBody = document.getElementById('verified-documents');
        //     tableBody.innerHTML = '';

        //     const verifiedDocs = documents.filter(doc => doc.status === 'approved' || doc.status === 'rejected');

        //     verifiedDocs.forEach(doc => {
        //         const row = document.createElement('tr');
        //         const badgeClass = doc.status === 'approved' ? 'badge-approved' : 'badge-rejected';
        //         const statusText = doc.status.charAt(0).toUpperCase() + doc.status.slice(1);

        //         row.innerHTML = `
        //             <td>${doc.id}</td>
        //             <td>${doc.agency}</td>
        //             <td><i class="fas ${getDocumentIcon(doc.type)} document-type-icon"></i> ${doc.type}</td>
        //             <td>${doc.verifiedDate}</td>
        //             <td><span class="badge ${badgeClass}">${statusText}</span></td>
        //             <td>
        //                 <button class="btn btn-view" onclick="viewDocument('${doc.id}')">
        //                     <i class="fas fa-eye"></i> View
        //                 </button>
        //             </td>
        //         `;
        //         tableBody.appendChild(row);
        //     });
        // }

        // function populateAgencyFunctions() {
        //     const functionsContainer = document.getElementById('agency-functions');
        //     functionsContainer.innerHTML = '';

        //     agencyFunctions.forEach(func => {
        //         const functionCard = document.createElement('div');
        //         functionCard.className = 'function-card';

        //         const statusBadge = func.status === 'active' ?
        //             '<span class="badge badge-approved">Active</span>' :
        //             '<span class="badge badge-pending">Pending</span>';

        //         functionCard.innerHTML = `
        //             <div class="function-header">
        //                 <h3>${func.name}</h3>
        //                 ${statusBadge}
        //             </div>
        //             <p class="agency-name">By ${func.agency} | Added on ${func.dateAdded}</p>
        //             <div class="function-body">
        //                 <p>${func.description}</p>
        //             </div>
        //             <div style="margin-top: 15px;">
        //                 <button class="btn btn-view" onclick="viewFunctionDetails('${func.id}')">
        //                     <i class="fas fa-cog"></i> Manage
        //                 </button>
        //                 ${func.status === 'pending' ? `
        //                 <button class="btn btn-approve" onclick="openFunctionApproval('${func.id}')">
        //                     <i class="fas fa-check"></i> Approve
        //                 </button>` : ''}
        //             </div>
        //         `;

        //         functionsContainer.appendChild(functionCard);
        //     });
        // }

        // // Helper function to get appropriate icon for document type
        // function getDocumentIcon(docType) {
        //     if (docType.includes('License')) return 'fa-file-contract';
        //     if (docType.includes('Certificate')) return 'fa-certificate';
        //     if (docType.includes('Policy')) return 'fa-file-signature';
        //     return 'fa-file-alt';
        // }

        // // Document view and verification functions
        // let currentDocumentId = null;

        // function viewDocument(documentId) {
        //     currentDocumentId = documentId;
        //     const document = documents.find(doc => doc.id === documentId);

        //     if (document) {
        //         // Set modal title and basic info
        //         document.getElementById('modal-title').textContent = ⁠ ${document.type}: ${document.id} ⁠;
        //         document.getElementById('document-id').textContent = document.id;
        //         document.getElementById('document-agency').textContent = document.agency;
        //         document.getElementById('document-type').textContent = document.type;
        //         document.getElementById('document-date').textContent = document.date;

        //         // Set status with appropriate badge
        //         const statusEl = document.getElementById('document-status');
        //         statusEl.textContent = document.status.charAt(0).toUpperCase() + document.status.slice(1);
        //         statusEl.className = '';
        //         statusEl.classList.add('badge', document.status === 'approved' ? 'badge-approved' :
        //                               document.status === 'rejected' ? 'badge-rejected' : 'badge-pending');

        //         // Show/hide verification info based on status
        //         const verifiedByContainer = document.getElementById('verified-by-container');
        //         const notesDisplayContainer = document.getElementById('verification-notes-container');
        //         const verificationForm = document.getElementById('verification-form-container');
        //         const verificationFooter = document.getElementById('verification-footer');

        //         if (document.status !== 'pending') {
        //             verifiedByContainer.style.display = 'block';
        //             notesDisplayContainer.style.display = 'block';
        //             verificationForm.style.display = 'none';
        //             verificationFooter.style.display = 'none';

        //             document.getElementById('verification-notes-display').textContent = document.notes || 'No notes provided';
        //         } else {
        //             verifiedByContainer.style.display = 'none';
        //             notesDisplayContainer.style.display = 'none';
        //             verificationForm.style.display = 'block';
        //             verificationFooter.style.display = 'flex';

        //             document.getElementById('verification-notes-input').value = '';
        //         }

        //         // Load document preview based on file type
        //         const previewContainer = document.getElementById('document-preview-container');
        //         previewContainer.innerHTML = '';

        //         if (document.fileType === 'image') {
        //             const img = document.createElement('img');
        //             img.src = document.fileUrl;
        //             img.alt = ⁠ ${document.type} Preview ⁠;
        //             img.style.maxWidth = '100%';
        //             img.style.maxHeight = '400px';
        //             previewContainer.appendChild(img);
        //         } else if (document.fileType === 'pdf') {
        //             const embed = document.createElement('embed');
        //             embed.src = document.fileUrl;
        //             embed.type = 'application/pdf';
        //             embed.style.width = '100%';
        //             embed.style.height = '400px';
        //             previewContainer.appendChild(embed);

        //             const downloadLink = document.createElement('a');
        //             downloadLink.href = document.fileUrl;
        //             downloadLink.textContent = 'Download PDF';
        //             downloadLink.className = 'btn btn-view';
        //             downloadLink.style.marginTop = '10px';
        //             downloadLink.style.display = 'inline-block';
        //             downloadLink.target = '_blank';
        //             previewContainer.appendChild(document.createElement('br'));
        //             previewContainer.appendChild(downloadLink);
        //         } else {
        //             previewContainer.innerHTML = ⁠ <p>Preview not available for this file type. <a href="${document.fileUrl}" class="btn btn-view" target="_blank">Download File</a></p> ⁠;
        //         }

        //         // Show the modal
        //         document.getElementById('documentModal').style.display = 'block';
        //     }
        // }

        // function approveDocument() {
        //     if (!currentDocumentId) return;

        //     const documentIndex = documents.findIndex(doc => doc.id === currentDocumentId);
        //     if (documentIndex !== -1) {
        //         const notes = document.getElementById('verification-notes-input').value;

        //         documents[documentIndex].status = 'approved';
        //         documents[documentIndex].verifiedDate = new Date().toISOString().split('T')[0];
        //         documents[documentIndex].notes = notes;
        //         documents[documentIndex].verifiedBy = 'Admin User';

        //         // In a real app, this would be an API call
        //         alert(⁠ Document ${currentDocumentId} has been approved. ⁠);

        //         document.getElementById('documentModal').style.display = 'none';
        //         populatePendingDocuments();
        //         populateVerifiedDocuments();
        //     }
        // }

        // function rejectDocument() {
        //     if (!currentDocumentId) return;

        //     const documentIndex = documents.findIndex(doc => doc.id === currentDocumentId);
        //     if (documentIndex !== -1) {
        //         const notes = document.getElementById('verification-notes-input').value;

        //         documents[documentIndex].status = 'rejected';
        //         documents[documentIndex].verifiedDate = new Date().toISOString().split('T')[0];
        //         documents[documentIndex].notes = notes;
        //         documents[documentIndex].verifiedBy = 'Admin User';

        //         // In a real app, this would be an API call
        //         alert(⁠ Document ${currentDocumentId} has been rejected. ⁠);

        //         document.getElementById('documentModal').style.display = 'none';
        //         populatePendingDocuments();
        //         populateVerifiedDocuments();
        //     }
        // }

        // // Function management
        // let currentFunctionId = null;

        // function viewFunctionDetails(functionId) {
        //     currentFunctionId = functionId;
        //     const func = agencyFunctions.find(f => f.id === functionId);

        //     if (func) {
        //         document.getElementById('function-modal-title').textContent = func.name;
        //         document.getElementById('function-id').textContent = func.id;
        //         document.getElementById('function-name').textContent = func.name;
        //         document.getElementById('function-agency').textContent = func.agency;
        //         document.getElementById('function-date').textContent = func.dateAdded;
        //         document.getElementById('function-description-text').textContent = func.description;

        //         const statusEl = document.getElementById('function-status');
        //         statusEl.textContent = func.status.charAt(0).toUpperCase() + func.status.slice(1);
        //         statusEl.className = '';
        //         statusEl.classList.add('badge', func.status === 'active' ? 'badge-approved' : 'badge-pending');

        //         // Hide approval section for already approved functions
        //         if (func.status === 'active') {
        //             document.getElementById('function-verification-container').style.display = 'none';
        //             document.getElementById('approve-function-btn').style.display = 'none';
        //         } else {
        //             document.getElementById('function-verification-container').style.display = 'block';
        //             document.getElementById('approve-function-btn').style.display = 'inline-block';
        //         }

        //         document.getElementById('functionModal').style.display = 'block';
        //     }
        // }

        // function openFunctionApproval(functionId) {
        //     currentFunctionId = functionId;
        //     viewFunctionDetails(functionId);
        // }

        // function closeFunctionModal() {
        //     document.getElementById('functionModal').style.display = 'none';
        // }

        // function approveFunction() {
        //     if (!currentFunctionId) return;

        //     const functionIndex = agencyFunctions.findIndex(f => f.id === currentFunctionId);

        //     if (functionIndex !== -1) {
        //         const notes = document.getElementById('function-notes-input').value;

        //         agencyFunctions[functionIndex].status = 'active';
        //         agencyFunctions[functionIndex].notes = notes;

        //         // In a real app, this would be an API call
        //         alert(⁠ Function ${currentFunctionId} has been approved and is now active. ⁠);

        //         document.getElementById('functionModal').style.display = 'none';
        //         populateAgencyFunctions();
        //     }
        // }


        document.addEventListener('DOMContentLoaded', () => {
  const form = document.getElementById('registration-form');
  const successMessage = document.getElementById('success-message');

  // Get all form inputs
  const nameInput = document.getElementById('name');
  const emailInput = document.getElementById('email');
  const passwordInput = document.getElementById('password');
  const confirmPasswordInput = document.getElementById('confirm-password');

  // Get all error message elements
  const nameError = document.getElementById('name-error');
  const emailError = document.getElementById('email-error');
  const passwordError = document.getElementById('password-error');
  const confirmPasswordError = document.getElementById('confirm-password-error');

  // Validation functions
  const validateName = (name) => {
    if (!name) {
      return 'Name is required';
    }
    if (!/^[a-zA-Z\s]+$/.test(name)) {
      return 'Name should only contain letters and spaces';
    }
    return '';
  };

  const validateEmail = (email) => {
    if (!email) {
      return 'Email is required';
    }
    if (!email.includes('@')) {
      return 'Please provide a valid email address';
    }
    const domain = email.split('@')[1];
    if (domain !== 'gmail.com') {
      return 'Please use a Gmail address (@gmail.com)';
    }
    return '';
  };

  const validatePassword = (password) => {
    if (!password) {
      return 'Password is required';
    }
    const passwordRegex = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$/;
    if (!passwordRegex.test(password)) {
      return 'Password must be at least 8 characters long and contain at least one uppercase letter, one lowercase letter, one number, and one special character';
    }
    return '';
  };

  const validateConfirmPassword = (password, confirmPassword) => {
    if (!confirmPassword) {
      return 'Please confirm your password';
    }
    if (password !== confirmPassword) {
      return 'Passwords do not match';
    }
    return '';
  };

  // Show error message and highlight input
  const showError = (input, errorElement, errorMessage) => {
    input.classList.add('error');
    errorElement.textContent = errorMessage;
  };

  // Clear error message and remove highlight
  const clearError = (input, errorElement) => {
    input.classList.remove('error');
    errorElement.textContent = '';
  };

  // Real-time validation
  nameInput.addEventListener('input', () => {
    const error = validateName(nameInput.value);
    if (error) {
      showError(nameInput, nameError, error);
    } else {
      clearError(nameInput, nameError);
    }
  });

  emailInput.addEventListener('input', () => {
    const error = validateEmail(emailInput.value);
    if (error) {
      showError(emailInput, emailError, error);
    } else {
      clearError(emailInput, emailError);
    }
  });

  passwordInput.addEventListener('input', () => {
    const error = validatePassword(passwordInput.value);
    if (error) {
      showError(passwordInput, passwordError, error);
    } else {
      clearError(passwordInput, passwordError);
    }

    // Revalidate confirm password
    if (confirmPasswordInput.value) {
      const confirmError = validateConfirmPassword(passwordInput.value, confirmPasswordInput.value);
      if (confirmError) {
        showError(confirmPasswordInput, confirmPasswordError, confirmError);
      } else {
        clearError(confirmPasswordInput, confirmPasswordError);
      }
    }
  });

  confirmPasswordInput.addEventListener('input', () => {
    const error = validateConfirmPassword(passwordInput.value, confirmPasswordInput.value);
    if (error) {
      showError(confirmPasswordInput, confirmPasswordError, error);
    } else {
      clearError(confirmPasswordInput, confirmPasswordError);
    }
  });

  // Form submission
  form.addEventListener('submit', (e) => {
    e.preventDefault();

    // Validate all fields
    const nameError = validateName(nameInput.value);
    const emailError = validateEmail(emailInput.value);
    const passwordError = validatePassword(passwordInput.value);
    const confirmPasswordError = validateConfirmPassword(
      passwordInput.value,
      confirmPasswordInput.value
    );

    // Show all errors if any
    if (nameError) showError(nameInput, document.getElementById('name-error'), nameError);
    if (emailError) showError(emailInput, document.getElementById('email-error'), emailError);
    if (passwordError) showError(passwordInput, document.getElementById('password-error'), passwordError);
    if (confirmPasswordError) showError(confirmPasswordInput, document.getElementById('confirm-password-error'), confirmPasswordError);

    // If no errors, submit the form
    if (!nameError && !emailError && !passwordError && !confirmPasswordError) {
      // Hide the form and show success message
      form.style.display = 'none';
      successMessage.classList.remove('hidden');

      // Log the form data (in a real application, you would send this to a server)
      console.log('Form submitted:', {
        name: nameInput.value,
        email: emailInput.value,
        password: passwordInput.value
      });
    }
  });
});


    </script>
  <% if(page !== 'customer-login' && page !== 'customer-register' && page !== 'agency-login' && page !== 'agency-register') { %>
    <%- include('partials/footer') %>
  <% } %>
  <!-- Bootstrap 5 JS Bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/script.js"></script>
  <script src="/js/customer.js"></script>
  <!-- Chatbot Widget -->
  <%- include('partials/chatbot') %>
  <link rel="stylesheet" href="/css/chatbot.css">
  <script src="/js/chatbot.js"></script>
</body>
</html>